export interface Degree {
    id: number; // Unique identifier
    name: string; // Name of the degree (e.g., "Computer Science")
    code: string; // Code for the degree (2-4 uppercase letters)
    yearsToComplete: number; // Duration (3-4 years)
    active: boolean; // Whether the degree is active
  }
  