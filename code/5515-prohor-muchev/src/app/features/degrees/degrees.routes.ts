import { Routes } from '@angular/router';
import { DegreesComponent } from './degrees.component';

export const DEGREE_ROUTES: Routes = [
  { path: '', component: DegreesComponent }
];

export default DEGREE_ROUTES; 